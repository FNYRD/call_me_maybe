def main() -> None:
    """Print the package's placeholder greeting.

    Unused entry point left over from ``uv init``. The real program
    starts at ``src/__main__.py``, run as ``python -m src``.
    """
    print("Hello from call-me-maybe!")
