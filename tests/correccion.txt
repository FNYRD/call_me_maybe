El contrato de tests/blackbox_test_bloque_6.md cambió de fondo. Antes de seguir, releer el archivo completo — resumen de lo que cambió:

1. El sujeto de prueba ya no es Chat construida a mano — es el comando `python -m src` corrido como subproceso real (R1-R3, R11). Ningún test debe importar ni construir Chat directamente como caso principal; los que sí lo hacen (invariantes puntuales de R6) son la excepción, no la regla.
2. R5 (qué rechaza) ahora se mide por código de salida + logs/logs.json, no por excepciones capturadas de Chat.__init__.
3. --output no necesita existir de antemano, ni su carpeta — el programa crea todo. No es un caso de rechazo.
4. R6.3 corregido: ERROR en la salida es copia del log real ("Model entered an loop", "Model failed while replying", "Empty prompt", etc.) — nunca el texto del prompt repetido. Había un bug real en chat.py, ya corregido.
5. R6.6 precisado: solo "Model failed while replying" se reintenta (hasta 3 veces). "Model entered an loop", "Empty prompt" y cualquier fallo de contenido van directo al objeto {"prompt", "ERROR"}, sin reintentar.
6. R9 suma fn_unknown — función de escape inyectada en tiempo de ejecución, no vive en ningún archivo, válida como name.

Si algo que ya escribiste asume la versión vieja, corrígelo contra el archivo actual antes de seguir.
